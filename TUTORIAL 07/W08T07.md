# Tutorial 7 Queue

## WONG YAN WEN (25005619)

### Question 1:
Name four everyday examples of a queue other than those discussed during lecture.

```
FIXED ANSWER:
- Supermarket checkout
- Bus stop line
- Printer job queue
- customer support calls
```

### Question 2:
What is the difference between a queue and stack?

FIXED ANSWER: 

||QUEUE|STACK|
|--|--|--|
|Insertion|Enqueue at rear|Push on top|
|Removal|Dequeue from front FIFO|Pop from top LIFO|

### Question 3:
Use the following code segment to answer parts (a) through (c): 

``` 
Queue<Integer> q = new Queue<Integer>(); 
Scanner keyIn = new Scanner(System.in); 
for (int i = 1; i <= 5; i++) 
{ 
    if (keyIn.nextBoolean()) 
        System.out.print(i + " "); 
    else 
        q.enqueue(i); 
    } 
} 
while (!q.isEmpty()) 
    System.out.print(q.dequeue() + " "); 
System.out.println(); 
```

(a) What is the output for the following input sequence?   ` true  false  false  true  true`

```
ANSWER:
1 4 5 2 3
```
 
(b) Is it possible to have output: 1 3 5 4 2? If yes, give an input sequence that produces the output; or else, provide justification to your answer. 
 
```
FIXED ANSWER:
No. 
The output must be : [true values in increasing order]
then [false/enqueued values in increasing order]
The suffix 4 2 is decreasing, so 1 3 5 4 2 cannot be produced.

```
 
(c) Give at least three input sequences that produce the output:   1 2 3 4 5 

```
FIXED ANSWER:
1. true true true true true
2. false false false false false
3. true true false false false
4. true false false false false
```

### Question 4:
Hand trace a queue X through the following operations:

``` 
X.enqueue(new Integer(14)); 
X.enqueue(new Integer(3)); 
X.enqueue(new Integer(5)); //14 3 5 
Object Y = X.dequeue();     //Y = 14 , X = 3 5
X.enqueue(new Integer(7));  //3 5 7
X.enqueue(new Integer(9)); //3 5 7 9
Y = X.dequeue();            //Y=3 , X = 5 7 9
X.enqueue(new Integer(2));  // 5 7 9 2 
X.enqueue(new Integer(4));  // 5 7 9 2 4
```


Given the resulting queue X above, what would be the result of each of the following?

a) `X.front();` 

```
FIXED ANSWER:  Front element is 5. Queue unchanged.
```

b) 

```
Y = X.dequeue(); 
X.enqueue(new Integer(10)); 
X.front(); 
```

```
ANSWER: 
Y = 5
X = 7 9 2 4 10
X.front() = 7 
```

c) `Y = X.dequeue();` 

```
FIXED ANSWER: Y = 7
X becomes [9, 2, 4, 10]

```

d) `X.front();`

```
FIXED ANSWER: Front element is now 9
```

### Question 5: 
Provide and explain three operations / functions of Linked List based Queue that you can add to the GenericQueue class, other than those discussed in the lecture (i.e., enqueue, dequeue and getSize – from Slide 10 in the lecture slide).

```
FIXED ANSWER:
// obtaining the element at the front of the queue
public E peek(){ 
    if (list.isEmpty())
        return null;
    
    return list.getFirst();}

// clears the queue
public void clear(){ list.clear(); }

// check whether the queue has no elements
public boolean isEmpty (){ return list.isEmpty(); }
```